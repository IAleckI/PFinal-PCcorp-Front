import Style from './cardListCart.module.css'

import { useQuery } from '@apollo/client'

export default function CardListCart(props) {
    
    return (
        <section className={Style.cardListCart}>

        </section>
    )
}